daemon off;
worker_processes 2;
pid /tmp/sentinel_nginx.pid;
error_log /dev/null;
events { worker_connections 512; }
http {
  access_log off;
  server {
    listen 8765;
    location / { return 200 "ok\n"; }
  }
}
